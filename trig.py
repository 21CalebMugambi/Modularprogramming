import math
#find sine
def get_sine(x):
    #x in degrees
    angle_rad=math.radians(x)
    angle_sin=math.sin(angle_rad)
    return angle_sin

def get_cos(x):
    #x in degrees
    angle_rad=math.radians(x)
    angle_cos=math.sin(angle_rad)
    return angle_cos
